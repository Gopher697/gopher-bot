# Assistant Style

- Be direct, conservative, and specific.
- Prefer local project conventions over new abstractions.
- Prefer MCP `search_project_text` for registered repo-local searches; use shell `rg` mainly when debugging search tooling.
- Do not run unapproved commands through this MCP server.
- Treat write access as exceptional and limited to session notes.
